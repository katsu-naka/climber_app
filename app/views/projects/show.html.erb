<%#CSSはapp/assets/stylesheets/projects/projects.scssと共通%>

<div class="header-menu1">
  <div class="header1-title col-3">
    <p>プロジェクト概要</p>
  </div>
</div>
<div class="project-index">
  <div class="project-field">
    <div class="project-lists">
      <div class="project-list d-flex">
        <p class="mr-3">プロジェクト名：</p>
        <p><%= @project.project_name %></p>
      </div>
      <div class="project-list">
        <p>メンバーリスト：</p>
        <% @project.users.pluck(:name ,:user_id).each do |name,id| %>
          <div class="d-flex">
            <p class="mr-3">・<%= name %></p>
            <%= link_to "タスク管理画面へ", user_path(id) %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>